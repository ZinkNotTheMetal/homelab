---
- hosts: plex
  vars_files:
    - "common.vars.yml"
    - "docker.vars.yml"
    - "plex.vars.yml"
    - "domain.vars.yml"
  roles:
    - role: common
    - role: mashimom.oh-my-zsh
      ohmyzsh_theme: "bureau"
    - role: docker
    - role: plex
      tag:
        - plex
