---
# HTTPRoute for hoarder.zinkzone.tech (Gateway API)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: karakeep
  namespace: karakeep
  annotations:
    external-dns.alpha.kubernetes.io/hostname: hoarder.zinkzone.tech
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: envoy-gateway-system
  hostnames:
    - hoarder.zinkzone.tech
  rules:
    - backendRefs:
        - name: karakeep
          port: 3000
